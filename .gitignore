# Binaries
*.exe
*.dll
*.so
*.dylib
bell.exe
bell_linux
bell_arm64
smartbell*
*.pdb

# Build artifacts (cross-compiled binaries + deploy folders)
smartbell_v*_linux_*
deploy_*/
dist/

# Database (Local data)
*.db
database.db
database.db-journal

# Uploads (Keep structure but ignore content)
public/assets/audio/*
!public/assets/audio/.gitkeep
public/assets/photos/*
!public/assets/photos/.gitkeep

# Logs (runtime, keep dir via .gitkeep)
logs/*
!logs/.gitkeep

# Config (contains secrets)
config.yaml

# Context (design references, not source)
context/

# System
.DS_Store
Thumbs.db
tmp/
progress.md
