# Database Configuration
DB_DRIVER=sqlite
# DB_DRIVER=mysql

# MySQL Configuration (if DB_DRIVER=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=belsekolah

# Server Configuration
PORT=8080

# Application Secret
SECRET_KEY=admin-secret-key-123

# Application Version (injected at build time)
APP_VERSION=2.1.0
APP_NAME=NIBA SuperApps
BUILD_DATE=2026-09-16
GIT_COMMIT=unknown
